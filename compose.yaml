services:
  web:
    build: .
    container_name: my-website
    restart: unless-stopped
    ports:
      - "8080:80"

    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s